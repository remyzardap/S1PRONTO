import { Router } from "express";

const router = Router();

// Routes for the agent can be added here
// router.get("/", ...);
// router.post("/", ...);

export const agentRouter = router;
export default router;
